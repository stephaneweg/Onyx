' hello.bas -- the smallest program. From the terminal:  basic hello.bas
' (text goes to the console); from QBasic or the File Viewer: in a window.
PRINT "Hello, world!"
FOR i = 1 TO 5
  PRINT "Line"; i; "squared is"; i * i
NEXT
