# Onyx IRC client settings. Plain-text IRC only (no TLS yet) -- use a port that
# allows cleartext (commonly 6667). Edit, then relaunch irc.
[irc]
server   = irc.libera.chat
port     = 6667
nick     = onyx-user
user     = onyx
realname = Onyx IRC user
channel  = #onyx
