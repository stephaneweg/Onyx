# autostart.txt -- apps started automatically at boot, one per line.
# Each name is the folder basename under /apps ("panel" => apps/panel.app/main.elf).
# Blank lines and lines starting with '#' are ignored.
#
# The panel is the shell: it draws the left sidebar, sets the wallpaper, and opens
# the app-list popup. Add more names here to auto-start additional apps.
panel
