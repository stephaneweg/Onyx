# autostart.txt -- apps started automatically at boot, one per line.
# Each name is the folder basename under /apps ("panel" => apps/panel.app/main.elf).
# Blank lines and lines starting with '#' are ignored.
#
# voronoy paints the desktop wallpaper into the kernel's shared wallpaper buffer,
# then exits. The panel is the shell: sidebar + taskbar + app-list popup. Add more
# names here to auto-start additional apps.
voronoy
panel
