# Upstream Circle dependency: cloned separately, has its own git history.
# (Will be promoted to a git submodule when we set up the build.)
# Clone with:  git clone --recurse-submodules https://github.com/rsta2/circle.git
/circle/

# Build artifacts
*.o
*.a
*.d
*.img
*.elf
*.lst
*.map
*.bin

# SD staging: firmware blobs are large/redistributable; keep README + config.txt
*.dat
*.dtb

# Editor / tooling
.claude/settings.local.json
